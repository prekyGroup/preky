spring:
  profiles:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/dbclient
    username: root
    password: tiger
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true